# GainzAssist Project Context

- Android App for tracking workouts.
- Tech Stack: Kotlin, Java, Room Database, Firebase Auth, Realtime Database.
- Old architecture uses Activities heavily with XML layouts, ViewBinding, and traditional `onActivityResult`.
- We are migrating the UI to Jetpack Compose incrementally while keeping the business logic intact.
