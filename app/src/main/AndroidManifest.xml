<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BlueTheme">

        <!-- Login Activity -->
        <activity
            android:name=".feature.login.presentation.screen.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>

        <!-- Facebook Login Config and Activity -->
        <!-- ================================================================================== -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="${FACEBOOK_APP_ID}" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="${FACEBOOK_CLIENT_TOKEN}" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:exported="false" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="${FB_LOGIN_PROTOCOL_SCHEME}" />
            </intent-filter>
        </activity>

        <!-- Facebook==login==config=========================================================== -->


        <!-- Main Activity -->
        <activity
            android:name=".feature.main.presentation.screen.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan|stateHidden" />

        <!-- HowToVideos Activity -->
        <activity
            android:name=".feature.how_to_videos.presentation.screen.HowToVideosActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:hardwareAccelerated="true"
            android:exported="false" />

        <!-- StartWorkout Activity -->
        <activity
            android:name=".feature.start_workout.presentation.screen.StartWorkoutActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan|stateHidden" />

        <!-- Summary Activity -->
        <activity
            android:name=".feature.summary.presentation.screen.SummaryActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity
            android:name=".feature.workout_entry.presentation.screen.WorkoutEntryActivity"
            android:exported="false" />
        <activity
            android:name=".feature.exercises_entry.presentation.screen.ExercisesEntryActivity"
            android:exported="false" />

        <service
            android:name=".data.service.FirebaseService"
            android:exported="false" />
    </application>

</manifest>